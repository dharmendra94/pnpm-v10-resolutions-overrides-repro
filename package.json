{
  "name": "pnpm-v10-resolutions-overrides-repro",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "repro": "pnpm run clean && pnpm install --lockfile-only --ignore-scripts --config.manage-package-manager-versions=false && node scripts/check-repro.mjs",
    "show-config": "pnpm config get overrides --config.manage-package-manager-versions=false"
  },
  "resolutions": {
    "is-odd": "3.0.1"
  }
}
